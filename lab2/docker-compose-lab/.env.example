# Copy this file to .env and fill in the values.
# docker-compose will automatically load a file named .env in the same directory.

POSTGRES_DB=appdb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

